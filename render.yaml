services:
  - type: web
    name: zyronexa
    env: python
    plan: free
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Zironexa:app --bind 0.0.0.0:$PORT --workers 1
    envVars:
      - key: FLASK_DEBUG
        value: "False"
